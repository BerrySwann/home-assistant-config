homeassistant:
  # Load packages
  packages: !include_dir_named integrations
  allowlist_external_dirs:
    - "/config/"
  


# Loads default set of integrations. Do not remove.
default_config:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes
automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml

# Pour powercalc
powercalc:

# Pour Cloudflared
http:
  use_x_forwarded_for: true

  trusted_proxies:
    - 172.30.0.0/16   # tous les add-ons / ingress / tunnels
    - 192.168.1.0/24  # ton LAN local
  # Protège l’accès à HA
  ip_ban_enabled: true
  # bannissement IP"
  login_attempts_threshold: 3
  # seuil de tentatives
  #clé privé / public
  #ssl_certificate: /ssl/local.crt
  #ssl_key: /ssl/local.key

#renvoi au fichier template.yaml
#template: !include template.yaml
template: !include_dir_merge_list templates/

# renvoi au fichier commad_line.yaml
command_line: !include command_line.yaml

#renvoi au fichier camera.yaml -> carte meteo france alerte
camera: !include camera.yaml

#renvoi au fichier utility_meter.yaml
utility_meter: !include utility_meter.yaml

#renvoi au fichier groupe
group: !include group.yaml

#revoin au fuchier input_number.yaml
input_number: !include input_number.yaml

#revoin au fuchier input_boolean.yaml
input_boolean: !include input_boolean.yaml

sensor: !include sensors.yaml

#renvoi au fichier shell_command.yaml
# Services shell_command (NOUVEAU)
shell_command: !include shell_command.yaml

#renvoi au fichier input_text.yaml
input_text: !include input_text.yaml

  # Amazon
alexa_media:
  accounts:
    - email: !secret amazon_user
      password: !secret amazon_password
      url: amazon.fr

  # Telegram
telegram_bot:
  - platform: polling
    api_key: !secret telegram_token
    allowed_chat_ids: 
    - !secret id_telegram_HA-Bot

notify:
  - platform: telegram
    name: Telegram
    chat_id: !secret id_telegram_HA-Bot
  - name: z2m_error_file
    platform: file
    filename: /config/zigbee2mqtt/log/error.log
    timestamp: false
    

# BASE DE DONNEE MariaDB
recorder:
  db_url: mysql://homeassistant:MariaDB@core-mariadb/homeassistant?charset=utf8mb4
  purge_keep_days: 32
  auto_purge: true
  auto_repack: true
  exclude:
    domains:
      - update
      - persistent_notification
      - sun
      - input_text

    event_types:
      - call_service

    entity_globs:
      - sensor.*linkquality
      - sensor.*last_seen
      - sensor.date*
      - sensor.time*

sql:
  - name: Taille DB Home Assistant
    # Crée sensor.taille_db_home_assistant"
    db_url: mysql://homeassistant:MariaDB@core-mariadb/homeassistant?charset=utf8mb4
    # core-mariadb : URL de la DB de l’add-on MariaDB"
    query: >
      SELECT Round(Sum(data_length + index_length) / POWER(1024,2), 1) AS value
      FROM information_schema.tables
      WHERE table_schema = "homeassistant";
    # Requête 'Database size' (doc officielle MariaDB/MySQL)"
    column: value
    unit_of_measurement: MiB
    device_class: data_size
    unique_id: taille_db_home_assistant

openai_conversation:
  api_key: !secret chat_gpt_api_key
  model: "gpt-4o-mini"

bluetooth:

  device_tracker:
  - platform: bluetooth_le_tracker


  - sensor:
      name: "SpeedTest CLI Data"
      unique_id: speedtest_cli_data
      command: "/config/3rdparty/speedtest/speedtest --format=json --accept-license --accept-gdpr"
      #Toutes les 4 heures, 60 * 60 * 4 = 14400  3600 = 1 heure
      scan_interval: 3600
      command_timeout: 60
      value_template: >-
        {{ 
          { 
            "ping": value_json.ping.latency, 
            "download": value_json.download.bandwidth, 
            "upload": value_json.upload.bandwidth 
          }
          | to_json 
        }}