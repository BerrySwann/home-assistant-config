- sensor:
  - name: "Message Clim personnalisé 20h 6h"
    unique_id: message_clim_personnalise_20h_6h
    state: >
      {# Définition des variables avec les entités confirmées #}
      {% set mamour = states('sensor.ne2213_mamour_network_type') %}
      {% set eric = states('sensor.poco_x7_pro_network_type') %}
      {% set tendance = states('sensor.th_balcon_nord_temperature_trend') %}
      {% set temp_ext = states('sensor.th_balcon_nord_temperature') | float %}
      {% set temp_cible = states('sensor.temperature_cible') | float %}
      {% set temp_diff = states('sensor.temperature_differentielle') | float %}
      {% set consigne_base = states('sensor.consigne_de_base') | float %}
      {% set tres_diff = (temp_diff + (temp_cible - consigne_base)) | round(1) %}
      {% set temp_eco = states('sensor.temperature_eco_hiver') | float %}
      {% set mode_ete_hiver = states('sensor.mode_ete_hiver') %}
      {% set prise_salon = is_state('switch.clim_salon_nous', 'on') %}
      {% set prise_bureau = is_state('switch.clim_du_bureau_nous', 'on') %}
      {% set prise_chambre = is_state('switch.clim_chambre_nous', 'on') %}
      {% set clims_alimentees = prise_salon or prise_bureau or prise_chambre %}
      {% set sant_clims = prise_salon and prise_bureau and prise_chambre %}

      {# Logique de génération des messages #}
      {% if mamour == 'cellular' and eric == 'cellular' %}
        Clim [2] en [CELLULAR]
        {% if mode_ete_hiver == 'heat' %}
          {% if sant_clims %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Les 3 Clim (remonter {{ temp_eco }}°) (mode hiver).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Les 3 Clim (remonter {{ temp_eco }}°) (mode hiver).
            {% else %}
              Temp. stable {{ temp_ext }}° Les 3 Clim (remonter {{ temp_eco }}°) (mode hiver).
            {% endif %}
          {% else %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° (prises éteintes).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° (prises éteintes).
            {% else %}
              Temp. stable {{ temp_ext }}° (prises éteintes).
            {% endif %}
          {% endif %}
        {% elif mode_ete_hiver == 'cool' %}
          {% if sant_clims %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Les 3 Clim {{ temp_cible }}° (mode été).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Les 3 Clim {{ temp_cible }}° (mode été).
            {% else %}
              Temp. stable {{ temp_ext }}° Les 3 Clim {{ temp_cible }}° (mode été).
            {% endif %}
          {% else %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° (prises éteintes).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° (prises éteintes).
            {% else %}
              Temp. stable {{ temp_ext }}° (prises éteintes).
            {% endif %}
          {% endif %}
        {% else %} {# mode_ete_hiver == 'fan_only' #}
          {% if sant_clims %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Les 3 Clim en mode ventilation.
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Les 3 Clim en mode ventilation.
            {% else %}
              Temp. stable {{ temp_ext }}° Les 3 Clim en mode ventilation.
            {% endif %}
          {% else %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° (prises éteintes).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° (prises éteintes).
            {% else %}
              Temp. stable {{ temp_ext }}° (prises éteintes).
            {% endif %}
          {% endif %}
        {% endif %}

      {% elif mamour == 'wifi' and eric == 'cellular' %}
        Clim [MAMOUR] en [WIFI]
        {% if clims_alimentees %}
          {% if mode_ete_hiver == 'heat' %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau {{ temp_cible }}° (mode hiver).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau {{ temp_cible }}° (mode hiver).
            {% else %}
              Temp. stable {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau {{ temp_cible }}° (mode hiver).
            {% endif %}
          {% elif mode_ete_hiver == 'cool' %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau {{ temp_cible }}° (mode été).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau {{ temp_cible }}° (mode été).
            {% else %}
              Temp. stable {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau {{ temp_cible }}° (mode été).
            {% endif %}
          {% else %} {# mode_ete_hiver == 'fan_only' #}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau en mode ventilation.
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau en mode ventilation.
            {% else %}
              Temp. stable {{ temp_ext }}° Chambre {{ tres_diff }}° - Salon & Bureau en mode ventilation.
            {% endif %}
          {% endif %}
        {% else %}
          {% if tendance == 'increasing' %}
            T°Ext Up >>> {{ temp_ext }}° (prises éteintes).
          {% elif tendance == 'decreasing' %}
            T°Ext Down <<< {{ temp_ext }}° (prises éteintes).
          {% else %}
            Temp. stable {{ temp_ext }}° (prises éteintes).
          {% endif %}
        {% endif %}

      {% elif mamour == 'cellular' and eric == 'wifi' %}
        Clim [ERIC] en [WIFI]
        {% if clims_alimentees %}
          {% if mode_ete_hiver == 'heat' %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre {{ temp_cible }}° (mode hiver).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre {{ temp_cible }}° (mode hiver).
            {% else %}
              Temp. stable {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre {{ temp_cible }}° (mode hiver).
            {% endif %}
          {% elif mode_ete_hiver == 'cool' %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre {{ temp_cible }}° (mode été).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre {{ temp_cible }}° (mode été).
            {% else %}
              Temp. stable {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre {{ temp_cible }}° (mode été).
            {% endif %}
          {% else %} {# mode_ete_hiver == 'fan_only' #}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre en mode ventilation.
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre en mode ventilation.
            {% else %}
              Temp. stable {{ temp_ext }}° Bureau {{ tres_diff }}° - Salon & Chambre en mode ventilation.
            {% endif %}
          {% endif %}
        {% else %}
          {% if tendance == 'increasing' %}
            T°Ext Up >>> {{ temp_ext }}° (prises éteintes).
          {% elif tendance == 'decreasing' %}
            T°Ext Down <<< {{ temp_ext }}° (prises éteintes).
          {% else %}
            Temp. stable {{ temp_ext }}° (prises éteintes).
          {% endif %}
        {% endif %}

      {% elif mamour == 'wifi' and eric == 'wifi' %}
        Clim [2] en [WIFI]
        {% if clims_alimentees %}
          {% if mode_ete_hiver == 'heat' %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Chambre {{ tres_diff }}° Salon & Bureau {{ temp_cible }}° (mode hiver).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Chambre {{ tres_diff }}° Salon & Bureau {{ temp_cible }}° (mode hiver).
            {% else %}
              Temp. stable {{ temp_ext }}° Chambre {{ tres_diff }}° & Salon & Bureau {{ temp_cible }}° (mode hiver).
            {% endif %}
          {% elif mode_ete_hiver == 'cool' %}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Chambre {{ tres_diff }}° Salon & Bureau {{ temp_cible }}° (mode été).
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Chambre {{ tres_diff }}° Salon & Bureau {{ temp_cible }}° (mode été).
            {% else %}
              Temp. stable {{ temp_ext }}° Chambre {{ tres_diff }}° & Salon & Bureau {{ temp_cible }}° (mode été).
            {% endif %}
          {% else %} {# mode_ete_hiver == 'fan_only' #}
            {% if tendance == 'increasing' %}
              T°Ext Up >>> {{ temp_ext }}° Chambre {{ tres_diff }}° & Salon & Bureau en mode ventilation.
            {% elif tendance == 'decreasing' %}
              T°Ext Down <<< {{ temp_ext }}° Chambre {{ tres_diff }}° & Salon & Bureau en mode ventilation.
            {% else %}
              Temp. stable {{ temp_ext }}° Chambre {{ tres_diff }}° & Salon & Bureau en mode ventilation.
            {% endif %}
          {% endif %}
        {% else %}
          {% if tendance == 'increasing' %}
            T°Ext Up >>> {{ temp_ext }}° (prises éteintes).
          {% elif tendance == 'decreasing' %}
            T°Ext Down <<< {{ temp_ext }}° (prises éteintes).
          {% else %}
            Temp. stable {{ temp_ext }}° (prises éteintes).
          {% endif %}
        {% endif %}

      {% else %}
        Aucun message pertinent actuellement.
      {% endif %}
